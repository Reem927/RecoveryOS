# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables — never commit real keys
.env*.local
.env
.env.local

# Supabase — migrations and local config stay off the public repo
/supabase

# Common ignores
node_modules
.next/
.DS_Store
.worktrees/

# Clerk configuration (can include secrets)
/.clerk/

# Claude Code local settings (permissions, personal config)
.claude/

# Build artifacts
tsconfig.tsbuildinfo

# Local backup files
*.local-bak

# Root-level SVG duplicates (served from /public instead)
/sona_final_line.svg
